# Current Version
v0.1.0

# Current Milestone
Recovery Milestone R9b — Atomic Bulk Channel Import
(implemented, tested, reviewed, committed, and pushed — see docs/HANDOFFS/R9b_atomic_bulk_channel_import_handoff.txt; implementation commit `e9b49df9c00d6d7e9b5d088711932a0d3e22bf6b` — "Implement atomic bulk channel import", pushed as a normal fast-forward from Recovery Milestone R9a's own implementation commit `387d2aa1166bd74527da7565f6c3ec4a1367f451` — "Implement channel import backend contracts"). Local `main` and `origin/main` are synchronized at `e9b49df9c00d6d7e9b5d088711932a0d3e22bf6b`, 0 ahead / 0 behind. R9a and R9b are both complete; the Trader-Performance Dashboard module (Recovery Milestone R8, R8a+R8b) is also fully complete and pushed. Recovery Milestone R9c — Bulk Channel Import UI — is the next and final sub-milestone of the Bulk Channel Import module (Recovery Milestone R9) and has not started; Recovery Milestone R9 as a whole is **not** yet complete.

# Completed
- Project structure
- VS Code environment
- GitHub repository
- Streamlit UI prototype
- Discord parser prototype
- Editable trade table
- Validation warning
- Milestone 2A: Version 1 database design (see docs/DATABASE_DESIGN_V1.md)
- Milestone 2B.1: database/schema.sql implemented and validated (see docs/HANDOFFS/2B.1_schema.md)
- Milestone 2B.2: database/config.py implemented and validated (see docs/HANDOFFS/2B.2_config.md)
- Milestone 2B.3: database/db.py implemented and validated (see docs/HANDOFFS/2B.3_db.md; commit ae3ebf07d0b2784c2fd849b7174be210e51bfc76)
- Milestone 2B.4: database/models.py implemented and validated (see docs/HANDOFFS/2B.4_models.md; commit 740e2556b6143d7e0fd983df5f325c516a7b506d)
- Milestone 2B.5a: database/repository.py sources access implemented and validated (see docs/HANDOFFS/2B.5a_sources_repository.md; commit 9afe549)
- Milestone 2B.5b: database/repository.py traders access implemented and validated (see docs/HANDOFFS/2B.5b_traders_repository.md; commit d03eae5)
  - `create_trader()`, `get_trader_by_external_id()`, `get_traders_by_name()`
  - 27/27 tests passing
- Milestone 2B.5c: database/repository.py raw_messages access implemented and validated (see docs/HANDOFFS/2B.5c_messages_repository.md; commit 9aa33e4)
  - `create_raw_message()`, `get_raw_message_by_external_id()`, `get_raw_messages_by_content_hash()`
  - 47/47 tests passing
- Milestone 2B.5d: database/repository.py trade_signals access implemented and validated (see docs/HANDOFFS/2B.5d_trades_repository.md; commit 04e72b6)
  - `create_trade_signal()`, `get_trade_signal_by_id()`, `update_trade_signal()`
  - 82/82 tests passing
- Milestone 2B.5e: database/repository.py trade_signal_edits access implemented and validated (see docs/HANDOFFS/2B.5e_trade_signal_edits_repository.md; commit df0b36f055be1f6199d53e2e98ceafba5634f554)
  - `create_trade_signal_edit()`, `get_trade_signal_edits()`
  - 100/100 tests passing
- Milestone 2B.6a: database/service.py TradeService scaffold + duplicate detection implemented and validated (see docs/HANDOFFS/2B.6a_trade_service_duplicate_detection.md; commit 799506bee7c3e320fb1d25044cbf2382637f5511)
  - `TradeService.check_duplicate_signal()`; repository addition: `get_trade_signals_matching()`
  - 133/133 tests passing
- Milestone 2B.6b: database/service.py edit-on-update rule implemented and validated (see docs/HANDOFFS/2B.6b_trade_service_edit_on_update.md; commit 1ae5dad52b7821ddf5fb92fcd327e2557969658e)
  - `TradeService.update_trade_signal()`; repository addition: `validate_trade_signal_update_fields()` shared validation helper
  - 141/141 tests passing
- Milestone 2B.6c: database/service.py ingest_message entry point implemented and validated (see docs/HANDOFFS/2B.6c_trade_service_ingest_message.md; commit bb75326c53e09851465747a8bac58c506822456f)
  - `TradeService.ingest_message()`; no repository changes
  - 158/158 tests passing
- Milestone 2B.7: Integration and transaction tests implemented and validated (see docs/HANDOFFS/2B.7_integration_transaction_tests.md; commit 44ffe33486ca7c71f27895a47fce547ba8b4fb80)
  - `tests/test_integration_transactions.py`; no production code changes
  - 163/163 tests passing
- Milestone 2C.1: app/parser.py implemented and validated (see docs/HANDOFFS/2C.1_parser_module.md; commit 3a2408ede8170155aced6d7b5a150f6a1804c62e)
  - `parse_message()`; source-independent, pure function, no database/UI/TradeService dependency
  - 191/191 tests passing
- Milestone 2C.2: app/app.py manual message entry UI implemented and validated (see docs/HANDOFFS/2C.2_manual_message_entry_ui.md; commit aa9917e3a102449697a76a68038324afa0320d19)
  - Manual raw-message text entry, invokes `app.parser.parse_message()`, displays structured result or "nothing found" message
  - `requirements.txt` added, pinning `streamlit==1.59.2`
  - No database, repository, TradeService, or persistence work included
  - 196/196 tests passing
- Milestone 2C.3: TradeService Integration — COMPLETE (see docs/HANDOFFS/2C.3_trade_service_integration.md; commit dfdc8b5ca98564ff398de177cec3a78a585bb380 — "Implement TradeService UI integration")
  - `app/app.py` wires the manual entry UI's parsed structured trade data to `TradeService.ingest_message()` via a fresh database connection created on each Submit click
  - Required "Trader display name" and "Stable external trader ID" UI fields; source name fixed to `"manual"`
  - Two-step Parse -> Review -> Submit workflow preserved, with `st.session_state`-based stale-preview protection
  - No repository, schema, service, or parser changes
  - 217/217 tests passing
- Milestone 2C.4: Integration Tests implemented and validated (see docs/HANDOFFS/2C.4_integration_tests.md; commit 6d0bfd2db5f4dbed4605676390716b9af81f0795 — "Add manual entry integration tests")
  - `tests/test_manual_entry_integration.py`; no production code changes
  - 8 new real-SQLite integration tests exercising the complete manual-entry path (Parse -> review -> Submit -> `TradeService` -> `repository.py` -> real temporary SQLite database), each against a unique temporary database with real schema initialization
  - Covers: full persisted-record and foreign-key verification across `sources`/`traders`/`raw_messages`/`trade_signals`; stable external trader identity reuse; advisory (non-blocking) duplicate warning with both signals persisted; malformed input creating no database rows; commit durability through a fresh connection; rollback removing all partial writes after a controlled mid-transaction failure; and protection against creating or modifying the repository-root `discord_traders.db`
  - 225/225 tests passing
  - Implementation commit `6d0bfd2` pushed to `origin/main`; documentation committed via commit `3be0c78` ("Update documentation for Milestone 2C.4"), also pushed to `origin/main` — Milestone 2C.4 is fully complete
- Phase 2D roadmap scoping approved: `docs/IMPLEMENTATION_ROADMAP.md` extended with Phase 2D — Manual Version 1 Operational Readiness (Milestones 2D.1 through 2D.7) and a Phase 2E — Discord Ingestion future-scope heading, plus an updated Implementation Order listing 2D.1-2D.7 (commit `1ed88d3628823ce231f76d9ad9e4f5a46b953a64`, pushed to `origin/main`; local `main` and `origin/main` synchronized at that commit)
  - Documentation only — no production code, test, schema, or architecture changes
  - 225/225 tests still passing
- Milestone 2D.1: Runtime Configuration and Application Data Locations implemented, tested, reviewed, committed, and pushed (see docs/HANDOFFS/2D.1_runtime_configuration_and_application_data_locations.md; commit 648ff121fc1f91a044f963f155fa0ea97e3211d6 — "Implement runtime database path configuration")
  - `database/config.py`: new `resolve_database_path()` — resolves a non-empty `DISCORD_TRADERS_DB_PATH` override (normalized via `Path(value).expanduser().resolve()`), else `%LOCALAPPDATA%\DiscordTraders\discord_traders.db`, else a `Path.home()\AppData\Local\DiscordTraders\discord_traders.db` fallback; `DatabaseConfig.db_path` remains required with no default
  - `database/db.py`: `initialize_database()` now creates required parent directories via `Path(config.db_path).parent.mkdir(parents=True, exist_ok=True)` immediately before opening its connection; directory-creation failures propagate normally
  - `app/app.py`: hardcoded `_DB_PATH` removed; `DatabaseConfig(db_path=resolve_database_path())` is the new call site; `TradeService.ingest_message()` remains the sole UI persistence entry point
  - No schema, model, parser, repository, or service redesign; no logging, backup, recovery, review UI, correction workflow, packaging, or Discord work included
  - 242/242 tests passing (225 pre-existing + 17 new across `tests/test_config.py`, `tests/test_db.py`, `tests/test_app.py`)
  - Implementation commit `648ff12` pushed to `origin/main`; local `main` and `origin/main` synchronized at that commit
- Milestone 2D.2: Operational Logging and Error Handling implemented, tested, reviewed, committed, and pushed (see docs/HANDOFFS/2D.2_operational_logging_and_error_handling.md; commit a0654e51af3f82ce8826107dad15bcc0f2944b35 — "Implement operational logging and error handling")
  - `app/logging_config.py` (new): standard-library-only logging, a `discord_traders` logger tree, independently idempotent `configure_console_logging()`/`configure_file_logging()` (each identified by its own private marker attribute), `resolve_log_path()` (fully independent of `database.config.resolve_database_path()`/`DISCORD_TRADERS_DB_PATH`), `sanitized_traceback()`, and `log_operation_failure()`
  - Console logging configured at application startup; file logging configured lazily at the start of the Submit workflow via a `RotatingFileHandler(maxBytes=1_000_000, backupCount=3, encoding="utf-8", mode="a", delay=True)`; file-logging failures never block submission and console logging remains available as fallback
  - `app/app.py`: broadened Submit exception boundary (`ValueError`, `TypeError`, `sqlite3.Error`, `OSError`, plus a final `except Exception` safety net), both logged at ERROR with sanitized diagnostics, no CRITICAL introduced; a new parser-failure guard distinguishes an unexpected exception (`"Could not parse this message."`) from the unchanged valid empty-result message (`"No trade signals found in this message."`); the unchanged `"Could not save the message to the database."` message covers all Submit failures; duplicate advisories remain a normal result, logged only at WARNING; rollback, unconditional connection cleanup, preserved review state, and `TradeService.ingest_message()` as the sole persistence entry point are all unchanged
  - Sanitized diagnostics exclude exception message text, absolute source paths, raw trade text, trader names, external trader IDs, parsed signal values, database/log paths, environment values, and SQL values
  - No schema, model, parser, repository, service, database-config, or database-initialization redesign; no backup, recovery, review UI, correction workflow, packaging, Discord, telemetry, or third-party dependency work included
  - 287/287 tests passing (242 pre-existing + 45 new across `tests/test_logging_config.py` and `tests/test_app.py`); tests use temporary log paths exclusively and do not write to the real LOCALAPPDATA log
  - Implementation commit `a0654e5` pushed to `origin/main`; local `main` and `origin/main` synchronized at that commit
- Milestone 2D.3: Database Backup and Recovery implemented, tested, reviewed, committed, and pushed (see docs/HANDOFFS/2D.3_database_backup_and_recovery.md; commit eb41fff5da5d80818b1b462685d4f5549eac1d58 — "Implement database backup and recovery")
  - `database/backup.py` (new): `create_backup()` using `sqlite3.Connection.backup()` for a consistent, standalone backup regardless of the source's rollback-journal or WAL journal mode; writes to a temporary file, verifies with `PRAGMA integrity_check`, then publishes via atomic rename; `resolve_backup_dir()` (override `DISCORD_TRADERS_BACKUP_PATH`, else `%LOCALAPPDATA%\DiscordTraders\backups`, else `Path.home()` fallback); UTC timestamped filenames (`discord_traders_YYYYMMDD_HHMMSS_ffffff.db`) with collision refusal; retention keeps the latest 10 verified backups (oldest deleted first, only after a successful verified backup, non-fatal deletion failures); `list_backups()`
  - `database/backup.py` restore (CLI-only, `python -m database.backup restore <path>`; never called from `app/app.py`): candidate validation (SQLite header, read-only open, integrity check, required-table/required-column schema compatibility) before any modification; a best-effort exclusive-access probe; a pre-restore safety backup (excluded from retention until post-restore validation succeeds); quarantine of any existing `-wal`/`-shm`/`-journal` sidecars, with a hard abort (production database and all sidecars left untouched) if a sidecar cannot be safely quarantined; atomic main-file replacement; post-restore integrity/schema validation with full recovery (original database and its quarantined sidecars restored) if validation fails
  - `app/app.py`: one new unconditional "Create Backup" button calling `database.backup.create_backup()`, with the same try/except/log/fixed-message pattern as the existing Submit workflow; no restore control; `TradeService.ingest_message()` remains the sole UI persistence entry point
  - No schema, repository, service, parser, review UI, correction workflow, packaging, or Discord redesign; `database/config.py`, `database/repository.py`, `database/service.py`, `database/schema.sql`, and `app/parser.py` are unchanged
  - 328/328 tests passing (287 pre-existing + 41 new across `tests/test_backup.py` (37) and `tests/test_app.py` (4), plus a 2-line assertion update in `tests/test_manual_entry_integration.py` for the always-rendered "Create Backup" button)
  - Implementation commit `eb41fff5da5d80818b1b462685d4f5549eac1d58` pushed to `origin/main`; documentation committed via commit `0c5257b244947b81be2f33294b9a0749cfae79a4` ("Update documentation for Milestone 2D.3"), also pushed to `origin/main` — Milestone 2D.3 is fully complete
- Milestone 2D.4: Stored-Signal Review UI implemented, tested, reviewed, committed, and pushed (see docs/HANDOFFS/2D.4_stored_signal_review_ui.md; commit 9aba2a2dce4e79384c52dd76db96600dd055d09c — "Implement stored-signal review UI")
  - `app/app.py`: a new `st.sidebar.radio("Workflow", ["Manual Message Entry", "Review Signals"])` control, with the entire script restructured into a plain `if`/`elif` on its value - not `st.tabs()`, since Streamlit executes every tab's body on every rerun regardless of visual selection - so only the selected workflow's code executes on a given rerun; the existing Parse/Submit/Create Backup logic moved unchanged in behavior into the `Manual Message Entry` branch
  - `app/app.py` `Review Signals` branch (new, strictly read-only): checks `Path(resolve_database_path()).exists()` before any database access - never calls `initialize_database()`, never opens a connection, never creates the directory or file, when the database does not yet exist - showing the fixed message `"No trade signals found."` in that case; otherwise opens a connection, delegates to `TradeService.list_trade_signals_for_review()` with source/trader/symbol text-input filters and an optional checkbox-gated date filter, and renders a read-only summary table (`st.dataframe`) plus a signal-ID `st.selectbox`-driven detail view (never depending on `st.dataframe`'s row-selection API) showing the raw message text only in the detail view; query/connection failures show the fixed message `"Could not load stored trade signals."`, logged via the existing sanitized `log_operation_failure()`
  - `database/repository.py` (additive): new `get_trade_signals_for_review()` joining `trade_signals`/`traders`/`raw_messages`/`sources`, `ORDER BY trade_signals.id DESC`, default `LIMIT 100`, with every active filter applied in SQL before `LIMIT` - exact `source_name`/`trader_name` match, case-insensitive exact `symbol` match, and a `trade_signals.created_at` date-range filter (inclusive day start, exclusive start of the following day); blank/`None` filters omit their `WHERE` fragment entirely; no existing repository function changed
  - `database/service.py` (additive): new `TradeService.list_trade_signals_for_review()`, a thin read-only delegation that only uppercases a non-blank `symbol` filter before calling the repository function and returns its result unchanged; no business rules, no writes; `TradeService` remains the application's sole persistence/service boundary and `app/app.py` still executes no raw SQL
  - Duplicate-warning status intentionally omitted from the review display - it is never persisted in the schema (only produced transiently at ingestion time), so it cannot be shown as stored data
  - No schema, parser, database-config, database-connection, backup, repository redesign, or service redesign; `database/schema.sql`, `database/config.py`, `database/db.py`, `database/backup.py`, and `app/parser.py` are all unchanged
  - 380/380 tests passing (328 pre-existing + 52 new: 21 in `tests/test_repository_sources.py`, 7 in `tests/test_service.py`, 20 in `tests/test_app.py` (16 covering navigation/empty/failure/display + 4 covering the date filter), and 4 real-SQLite integration tests in the new `tests/test_review_integration.py`)
  - Implementation commit `9aba2a2dce4e79384c52dd76db96600dd055d09c` pushed to `origin/main`; documentation committed via commit `ce1e6c527ae3e33e5bd3b915c8e4dc49ecc77328` ("Update documentation for Milestone 2D.4"), also pushed to `origin/main` — Milestone 2D.4 is fully complete
- Milestone 2D.5: Correction and Audit Workflow — implementation committed locally (see docs/HANDOFFS/2D.5_correction_and_audit_workflow.md; commit 644fb7a792e437c35d28e281fa9e5ffff9d1bee3 — "Implement correction and audit workflow"). **Implementation only; documentation for this milestone is not yet committed, and the implementation commit has not yet been pushed to `origin/main`. Milestone 2D.5 is not considered fully complete until the documentation commit is reviewed, approved, and committed.**
  - `database/service.py`: `TradeService.update_trade_signal()` extended with an optional keyword-only `expected_current_values: dict | None = None`, fully backward-compatible - when omitted, behavior is byte-for-byte the pre-existing Milestone 2B.6b sparse-update logic (every existing 2B.6b test passes unmodified); when supplied, the call is treated as a controlled correction requiring `changed_fields` and `expected_current_values` to each contain exactly the six approved fields (`symbol`, `action`, `option_type`, `price`, `expiration`, `position_size`), rejecting `raw_message_id`, `trader_id`, `id`, `created_at`, `updated_at`, and any missing/extra field
  - Controlled-correction check order: field-shape validation, then the shared repository field validation, then fetch the current signal (raising the new `TradeSignalNotFoundError`, a `ValueError` subclass, if missing - the legacy path's own missing-signal `ValueError` is unchanged), then a typed stale-value comparison (`Decimal`/`None`, never float, never a Decimal-vs-string mismatch) against a canonical current-value snapshot - raising the new `StaleTradeSignalError` on any mismatch - **before** a no-op comparison (raising a plain `ValueError` if the six proposed values are identical to the current ones) - both checks strictly **before** the existing full-row `trade_signal_edits` snapshot and the update are applied; no audit row is written and `updated_at` is not changed for any invalid, stale, missing, or no-op correction
  - `database/service.py` (additive): new `TradeService.list_trade_signal_audit_history()` - decodes and validates each `trade_signal_edits.previous_values` JSON value inside `TradeService` (raising the new `AuditHistoryError` for malformed or non-dict JSON), returning newest-first plain dicts with `id`, `edited_at`, and the six previous editable values (price preserved as its exact stored string); the raw JSON text itself is never returned or logged, and `app/app.py` never decodes audit JSON itself
  - `app/app.py`: a "Correct Signal" entry point inside the existing `Review Signals` branch, prefilling a correction form (symbol/price/expiration/position-size text inputs; action and option-type fixed `st.selectbox` controls - `BTO`/`STC`/`BTC`/`STO`/`BUY`/`SELL` and blank/`call`/`put` respectively) from the currently displayed values; symbol and action normalized to uppercase, option type to lowercase or `None`, price parsed as `Decimal`, expiration validated as `YYYY-MM-DD` - all client-side, before the correction service call (not before any connection is opened, since the branch already has one open for the review list/audit history at that point); a required confirmation checkbox gates Save; Save/Cancel and a read-only, newest-first "Correction History" section all share the one connection already opened for the review list within that rerun - no extra connection is opened and the database is never initialized here, preserving the Milestone 2D.4 no-implicit-creation guarantee exactly
  - Exact fixed messages: validation/no-op - `"Please enter a valid correction that changes at least one field."`; conflict (stale or not-found) - `"This trade signal changed or is no longer available. Reload it before correcting."`; persistence failure - `"Could not save the trade signal correction."`; success - `"Trade signal correction saved."`; audit-history failure - `"Could not load correction history."`
  - Session-state rules: entering correction mode captures the signal ID and canonical typed six-field snapshot; changing the selected signal, changing filters so the signal disappears, a stale/not-found conflict, or Cancel all clear correction state; a validation failure keeps the form open; a successful save clears correction state before the next rerun; the raw message stays `disabled=True`; no delete control exists anywhere in this workflow
  - Logging reuses the existing sanitized `log_operation_failure()` contract exactly - no raw message/field values, trader/source names, audit JSON, paths, or exception text in any log record or user-facing message; no `CRITICAL` logging introduced
  - No schema, repository, model, parser, database-config, or database-connection changes; `database/repository.py`, `database/schema.sql`, `database/models.py`, `database/config.py`, `database/db.py`, `database/backup.py`, `app/parser.py`, and `tests/test_repository_sources.py` are all unchanged
  - 433/433 tests passing (380 pre-existing + 53 new: 19 in `tests/test_service.py`, 22 in `tests/test_app.py`, and 12 real-SQLite integration tests in the new `tests/test_correction_integration.py`); all UI/integration tests isolate real file logging (patched or redirected) and the real `%LOCALAPPDATA%\DiscordTraders` log directory was confirmed absent both before and after the full suite run
  - Implementation commit `644fb7a792e437c35d28e281fa9e5ffff9d1bee3` committed locally; documentation committed via commit `aafa3c50052c07798a86ad203a8a60b056c58391` ("Update documentation for Milestone 2D.5"), also pushed to `origin/main` — Milestone 2D.5 is fully complete
- Milestone 2D.6: Windows Startup and Packaging implemented, tested, reviewed, committed, and pushed (see docs/HANDOFFS/2D.6_windows_startup_and_packaging.md; commit 46c77830793d42fb869f202e973e41cfd4dfd34d — "Implement Windows startup and packaging")
  - `start.bat` (new): a repo-root, double-clickable Windows launcher requiring no VS Code, terminal, or Claude Code to be open; resolves its own directory via `%~dp0`; detects Python via the `py -3` launcher first, falling back to bare `python`; creates a repo-local `.venv` only if one does not already exist and never requires venv activation; installs `requirements.txt` only on first run or when `streamlit` is not yet importable in the existing venv, so later runs never reinstall unnecessarily; starts the app via `.venv\Scripts\python.exe -m streamlit run app/streamlit_app.py --server.showEmailPrompt=false`; every failure point (Python missing, venv-creation failure, dependency-install failure, Streamlit-startup failure) shows a clear on-screen message and pauses before exiting; a visible console window is an accepted V1 tradeoff
  - `--server.showEmailPrompt=false` suppresses Streamlit's one-time-per-machine interactive "onboarding email" prompt, which would otherwise block startup indefinitely for a non-technical user with no attached stdin; no global user configuration file (`%USERPROFILE%\.streamlit\credentials.toml`) is ever created or modified; automatic browser opening is unchanged (no `--server.headless`)
  - `app/app.py` renamed to `app/streamlit_app.py` (via `git mv`; content-identical apart from one self-referential docstring line) — a structural fix for a fully proven Streamlit import-collision bug: Streamlit's own bootstrap (`_fix_sys_path()`) inserts the target script's containing directory at `sys.path[0]`, and since the entry point was named `app/app.py`, that inserted directory (`app/`) itself contained a file named `app.py`, causing `from app.logging_config import ...` to resolve `app` to that file (not a package) instead of the real `app` package at the repo root, raising `ModuleNotFoundError: No module named 'app.logging_config'; 'app' is not a package`; root cause proven via direct inspection of Streamlit's installed source plus an empirical `importlib.util.find_spec('app')` reproduction before any fix was proposed; the rename eliminates the collision with no business-logic, database, parser, or service change
  - `database/backup.py`, `tests/test_app.py`, `tests/test_correction_integration.py`, `tests/test_manual_entry_integration.py`, `tests/test_parser.py`, `tests/test_review_integration.py` updated only for the rename (docstring references and `AppTest.from_file("app/app.py")` calls -> `"app/streamlit_app.py"`; the parser import-independence guard string updated to match); no test logic changed
  - `.gitignore` (new): single entry, `.venv/`, so the repo-local virtual environment is never committed; `README.md`: added a short non-technical "Starting the app (Windows)" section
  - Real end-to-end verification performed (not just code review): `start.bat` executed for real multiple times across the milestone, confirming existing-venv reuse with no unnecessary reinstall, no email prompt, no `ModuleNotFoundError`, Streamlit reaching ready state, and `Local URL: http://localhost:8501` produced; every spawned process (launcher `cmd.exe`, venv `python.exe`, Streamlit's nested interpreter) confirmed terminated and port 8501 confirmed free after each run
  - No schema, repository, model, parser, service, database-config, database-connection, or `requirements.txt` change; standalone/PyInstaller packaging was evaluated during planning and explicitly rejected for V1; auto-start on boot, an installer, code-signing, auto-update, and Phase 2E/Discord work are all explicitly out of scope for this milestone
  - 433/433 tests passing (same 433 as Milestone 2D.5 — no new tests added; existing `AppTest.from_file(...)` calls and one existing assertion updated only for the rename, re-verified passing)
  - Implementation commit `46c77830793d42fb869f202e973e41cfd4dfd34d` pushed to `origin/main` as a normal fast-forward (`aafa3c5..46c7783`); local `main` and `origin/main` synchronized at that commit, 0 ahead / 0 behind — Milestone 2D.6 is fully complete
- Milestone 2D.7: Release and User Acceptance Testing — UAT execution complete (see docs/HANDOFFS/2D.7_release_and_user_acceptance_testing.txt; preparation commit `2d39b93c37324b0a3a1a4a6f413bc7469dec1d4e` — "Prepare release and UAT procedure"; no application code changed in this milestone)
  - `docs/RELEASE_CHECKLIST.md` (new): reusable, version-agnostic release/UAT checklist covering Windows Sandbox setup, every test below, pass/fail criteria, release-blocking criteria, a defect table, Product Owner sign-off, and `v0.1.0` tagging gates; `sandbox/` (new): `prepare_release_candidate.ps1` (host-side, creates a detached `git worktree` checkout of the exact release-candidate commit with full failure/verification handling and cleanup instructions, never modifying the live development working tree or branch), `discord_traders_uat.wsb` (Windows Sandbox config mapping that worktree read-only), `sandbox_logon_setup.bat` (copies the read-only content into a writable Sandbox-local folder with correct robocopy exit-code handling)
  - UAT executed against release-candidate commit `2d39b93c37324b0a3a1a4a6f413bc7469dec1d4e` inside two Windows Sandbox sessions (Session A: Python-missing test only; Session B: full happy path, kept open continuously across all remaining steps), with every step personally performed and reported by the Product Owner
  - All 13 UAT items passed: Session A Python-missing behavior; Session B first-run install and second-run venv reuse; database creation and restart persistence; valid and malformed message handling; duplicate advisory; review filters and read-only raw message; correction and audit history; stale/conflict protection; backup creation; disposable restore/recovery; invalid restore-candidate rejection; and logging/error-sanitization (log confirmed fully sanitized, no `CRITICAL` level)
  - No release-blocking defects found; no structural defect occurred; no fix of any kind was required during UAT execution
  - Windows Sandbox Sessions A and B both fully closed (all processes confirmed terminated); the release-candidate `git worktree` removed via `git worktree remove` + `git worktree prune`, confirmed no stale worktree administrative metadata remains; the live development working directory's branch and tracked files were never read from or modified by any of this milestone's Sandbox/worktree operations
  - 433/433 tests passing (unchanged from Milestone 2D.6 — no application, database, or test code was modified in this milestone)
  - Local `main` and `origin/main` synchronized at `2d39b93c37324b0a3a1a4a6f413bc7469dec1d4e` (0 ahead / 0 behind) throughout and after UAT; no `v0.1.0` tag exists yet
  - **Release status: ready for explicit Product Owner `v0.1.0` release approval** (a distinct, separate decision from the UAT-completion sign-off already recorded) — tagging has not occurred; **Phase 2E — Discord Ingestion has not started** and remains future scope; per the Product Owner's explicit instruction, project work stops after `v0.1.0` is released unless a new, separate, explicit approval is given later to begin Phase 2E
- Recovery Milestone R5: Metadata-rich ingestion, batch import, reprocessing, supersession, and channel checkpoints — implemented, tested, reviewed, committed, and pushed (see docs/HANDOFFS/R5_ingestion_reprocessing_and_checkpoints.txt; implementation commit `2bf81bedea5f501025389e952f52e70d9db2a384` — "Implement Recovery Milestone R5 ingestion and reprocessing"; documentation commit `9eb38ca22189e84720274fd5b5535680b6469234` — "Update documentation for Recovery Milestone R5")
  - New `TradeService` public methods `ingest_channel_message()`, `ingest_batch()`, `reprocess_raw_message()`, `reprocess_import_batch()`, `get_channel_checkpoints()`; no schema or migration changes
  - 826/826 tests passing (802 tests present after the R5 implementation, plus 24 new correction tests)
  - This is a Recovery-track milestone; the pre-Recovery `2D.x`/`v0.1.0` track's own historical Milestone entries above (through 2D.7) are left intact as a record of that track's completed, released work — this document's current-status fields (top-level Current Milestone/Current Focus/Next Milestone, and the Documentation block below) have been updated to reflect the Recovery Phase now in progress
  - Backend-only: the existing manual v0.1.0 application (`app/streamlit_app.py`) remains the only user-facing workflow and is unchanged by this milestone; the Product Owner's target workflow of pasting Discord messages directly into the application and viewing performance by trader is not yet achievable and requires later Recovery analytics and UI milestones (at minimum R6 lifecycle linking, R7 trader-performance analytics, and R8 UI)
- Recovery Milestones R6.1 through R6.4: lifecycle-linking foundation — implemented, committed, and pushed. No dedicated `docs/HANDOFFS/` entry exists for these four sub-milestones individually; they are recorded here for continuity. `database/schema.sql` itself is unchanged throughout — all four are additive via the existing `database/migrations/` framework and/or pure orchestration code.
  - R6.1 — lifecycle schema foundation: `trade_lifecycles`/`trade_lifecycle_events` tables and `trade_signals.lifecycle_id`, per `docs/DECISIONS/0002_trade_lifecycle_schema.md` (commit `603a2a94074b34e96b37a9c7b23e10c100dcb7d5` — "Add R6.1 lifecycle schema foundation")
  - R6.2 — pure lifecycle-matching engine in `database/lifecycle.py` (no database access) (commit `85a1efb979514d9b01eb8947089358bd793508db` — "Add R6.2 pure lifecycle-matching engine")
  - R6.3 — lifecycle repository/persistence layer in `database/repository.py` (commit `e3117cecd03d1b72b1f2d2baa0348038441f884a` — "Add R6.3 lifecycle repository layer")
  - R6.4 — `TradeService.rebuild_all_lifecycles()` / `TradeService.rebuild_lifecycles_for_raw_message_ids()` lifecycle rebuild orchestration, each owning one complete transaction (commit `4ac71a3bde3a06ebfc9be3bdef2a0cae03f9e68e` — "Implement lifecycle rebuild orchestration")
  - Backend-only, same as R5: no UI path to any lifecycle capability exists yet
- Recovery Milestone R6.5a: Lifecycle-Safe Trade-Signal Correction Service Contract — implemented, tested, reviewed, committed, and pushed (see docs/HANDOFFS/R6.5a_lifecycle_safe_trade_signal_correction.txt; commit `2018f2bb97a4c695c13cfd83f6245a7cd85aa57e` — "Implement lifecycle-safe trade signal correction")
  - New `TradeService.correct_trade_signal(trade_signal_id, expected_current_values, **changed_fields) -> TradeSignalCorrectionResult` — the lifecycle-aware counterpart to the pre-existing controlled-correction mode of `update_trade_signal()` (Milestone 2D.5), which remains completely unchanged and coexists unmodified
  - New immutable result model `TradeSignalCorrectionResult` (`database/models.py`) and new exception `LifecycleUnsafeCorrectionError` (`database/service.py`)
  - Owns exactly one atomic transaction via the existing `_r5_write_transaction()`: validate → fetch → reject stale/no-op → reject an unsafe `action` change on a lifecycle-managed signal → write one audit row → update the signal → rebuild affected lifecycles (via the existing private targeted no-commit rebuild path) when required, else run lifecycle-membership integrity validation directly → reload the authoritative signal → commit exactly once; any failure rolls back everything
  - Corrections to `symbol`/`option_type`/`expiration` trigger a targeted rebuild for a lifecycle-managed signal; corrections to `price`/`position_size` never rebuild but still validate membership integrity; a legacy signal found carrying unexpected stale lifecycle state is self-healed through the same targeted rebuild path
  - Backend-only: `app/streamlit_app.py` has not been migrated to this new API — that is Recovery Milestone R6.5b
  - Exactly three files changed: `database/models.py`, `database/service.py`, `tests/test_service_correction_lifecycle.py` (3 files changed, 975 insertions(+), 0 deletions(-))
  - 1126/1126 tests passing (26 new, focused, real-SQLite tests in `tests/test_service_correction_lifecycle.py`; 1100 pre-existing tests unchanged)
- Recovery Milestone R6.5b: Lifecycle-Safe Correction UI Integration — implemented, tested, reviewed, committed, and pushed (see docs/HANDOFFS/R6.5b_lifecycle_safe_correction_ui_integration.txt; commit `bd911ac04c12e45e3501202f45124d7233ebe723` — "Implement lifecycle-safe correction UI integration")
  - `app/streamlit_app.py`'s shipped "Correct Signal" save path migrated from `TradeService.update_trade_signal()`'s controlled-correction mode to the lifecycle-safe `TradeService.correct_trade_signal()` (Recovery Milestone R6.5a) - the only correction method the UI calls now; `TradeService.update_trade_signal()` remains available on `TradeService` for backward compatibility and its own direct tests (legacy sparse-update and Milestone 2D.5 controlled-correction mode) are completely unchanged
  - Transaction ownership moved to the service: `correct_trade_signal()` owns correction commit/rollback end to end (unchanged from R6.5a); the correction-save branch no longer calls `conn.commit()`/`conn.rollback()`; Manual Message Entry's own separate caller-owned ingestion transaction handling (its own `conn.commit()`/`conn.rollback()`) is unchanged
  - New exception-to-message mapping in the correction-save branch: `LifecycleUnsafeCorrectionError` and other correction-validation `ValueError` cases route to the existing validation message; `StaleTradeSignalError`/`TradeSignalNotFoundError` route to the existing conflict message; `LifecycleIntegrityError`/`LifecycleSnapshotError`/`TypeError`/`sqlite3.Error`/`OSError`/`RuntimeError`/any other unexpected exception route to the existing generic failure message; all five approved fixed messages (validation, conflict, failure, success, audit-history-failure) are byte-for-byte unchanged
  - Correction form state and every entered value are now preserved after every rejected save, including the stale-value and not-found conflict cases (which previously cleared the form under the pre-R6.5b UI); state clears only on a successful save, an explicit Cancel, a selected-signal change, or a filter change that removes the signal from view
  - Fixed-choice compatibility fix for a confirmed prior defect: the Recovery extractor persists actions including `BOUGHT`/`SOLD` (`app/parser.py`'s `extract_trade_event()`), which are absent from the six standard `_ACTION_CHOICES`; the pre-R6.5b UI defaulted an unrecognized persisted action to selectbox index 0 (`"BTO"`), risking a silent `BOUGHT`->`BTO` (or `SOLD`->`STC`) conversion on a correction that only touched an unrelated field. A new pure helper, `_correction_selectbox_choices()`, builds a fresh copy of the standard choice list and appends the persisted current value only when absent, selecting it by default; `_ACTION_CHOICES`/`_OPTION_TYPE_CHOICES` are never mutated, and no free-text action entry was introduced. The identical defensive pattern is applied to `option_type`
  - No lifecycle rebuild counters, lifecycle IDs, or other internal lifecycle details are exposed in the UI; correction failures continue through the existing sanitized `log_operation_failure()` contract - no raw messages, entered correction values, trader identifiers, database/log paths, exception message text, or lifecycle internals in any log record
  - Exactly three files changed: `app/streamlit_app.py`, `tests/test_app.py`, `tests/test_correction_integration.py` (3 files changed, 729 insertions(+), 122 deletions(-)); `database/service.py`, `database/models.py`, `database/repository.py`, `database/lifecycle.py`, and every file under `database/migrations/` are unchanged - the R6.5a service API was fully sufficient
  - 1146/1146 tests passing (20 new: 15 in `tests/test_app.py`'s `CorrectSignalWorkflowTests` - exact `correct_trade_signal()` call arguments, confirmation that `update_trade_signal()` is never called, no UI-side `conn.commit()`/`conn.rollback()`, every state-preservation and state-clearing path, every exception-category message mapping, and `BOUGHT`/`SOLD`/out-of-list-`option_type` appearance/selection/preservation; 5 real-SQLite integration tests in the new `LifecycleAwareUiCorrectionIntegrationTests` in `tests/test_correction_integration.py` - UI-driven `correct_trade_signal()` usage confirmed via a spy on the real method, fresh-connection commit durability with no test-side commit, a lifecycle-managed price-only correction, a `BOUGHT` action preserved through such a correction, a symbol correction triggering targeted lifecycle replacement with no dual current membership, and an atomic rejection of a managed action change leaving no partial mutation and the form open)
  - Implementation commit `bd911ac04c12e45e3501202f45124d7233ebe723` pushed to `origin/main` as a normal fast-forward (`a87fb9a..bd911ac`); local `main` and `origin/main` synchronized at that commit, 0 ahead / 0 behind. No new tag was created or moved; the existing `v0.1.0` tag remains unrelated and unchanged
  - Recovery Milestone R6.6: Real-Corpus Lifecycle Acceptance — implemented, tested, reviewed, committed, and pushed (see docs/HANDOFFS/R6.6_real_corpus_acceptance.txt; commit `1cd09a1b29eb8e1cc9abd4dde4d865dea0b8adf4` — "Add real-corpus lifecycle acceptance tests")
  - New `tests/test_lifecycle_corpus_acceptance.py` only — acceptance testing only; no parser, date/time-resolution, ingestion, lifecycle-engine, repository, service, schema, migration, or UI change of any kind
  - Exercises the complete real pipeline (`tests.discord_corpus_fixture.CORPUS` → `app.discord_adapter` → `app.parser.extract_trade_event()` → `app.datetime_resolution` → `TradeService.ingest_batch()` → `TradeService.rebuild_all_lifecycles()` → persisted `trade_lifecycles`/`trade_lifecycle_events` → `validate_lifecycle_membership_integrity()`) against the real 68-message Discord corpus, entirely through unique temporary file-backed SQLite databases via production `DatabaseConfig`/`initialize_database()`/`get_connection()`; the normal application database is never touched; no pipeline or lifecycle component is mocked
  - First-rebuild counters proven exact: `keys_considered=18, keys_changed=18, keys_unchanged=0, lifecycles_superseded=0, lifecycles_created=20, lifecycle_events_created=68, signal_pointers_cleared=0, signal_pointers_assigned=68`
  - Global invariants proven: 20 current lifecycle rows, 18 distinct complete lifecycle keys, 68 lifecycle-event memberships, 68 distinct signals with a lifecycle pointer, 0 eligible signals without one, 0 signals in more than one current lifecycle, `validate_lifecycle_membership_integrity() == []`; every one of the 68 corpus sequence numbers appears exactly once with no gap or duplicate; every lifecycle has at least one event with contiguous `sequence_index` values starting at 1; every event agrees with its signal's `lifecycle_id` pointer
  - Exact status distribution proven: `closed=12, partially_closed=4, open=1, orphan=2, unresolved=1, invalid=0`
  - All 20 lifecycle generations across all 18 distinct keys individually verified by name, including all seven TC generations (`IBM 207.5C`, `NVDA 207.5C`, the `QQQ 685P` unresolved-ADD/orphan-ALL-OUT singleton pair, `QQQ 687C`, `MU 950C`, `MU 955C`), Bdorts' two AVGO generations, spacemonkey's eight generations including the `SPX 7430P` stop-out-then-re-entry pair (two separate lifecycle generations, proven via member `sequence_in_batch`, never lifecycle ID), Matae's TSLA generation, and Sarang's two generations — every case identified by trader/symbol/option_type/Decimal-equivalent-strike/expiration plus stable corpus `sequence_in_batch` numbers, never database-generated IDs
  - Extraction-layer ambiguity (`message_extractions.ambiguity_flags`) and lifecycle-layer ambiguity (`trade_lifecycles.ambiguity_flags`) tested separately: exactly corpus sequences 13 and 34 carry the complete extraction flag list `["expiration_year_missing", "stated_return_missing"]`; exactly one current lifecycle (the TC `QQQ 685P` unresolved ADD singleton, sequence `[16]`) carries a non-empty lifecycle flag list, exactly `["ambiguous_add_no_open_position"]`; no lifecycle carries `fraction_exceeds_remaining`
  - A second `rebuild_all_lifecycles()` call proven a strict no-op: exact zero-write counters (`keys_considered=18, keys_changed=0, keys_unchanged=18`, every other counter `0`), with identical lifecycle IDs, identical total historical row/event counts, identical signal pointers, identical full lifecycle shapes, and identical immutable event snapshots proven before/after via fresh connections, not merely "still 20 current rows"
  - Quality-review corrections made before commit: (1) the implementation report's "TC ×6" tally was a reporting arithmetic error — the file always correctly contained all seven TC generation tests; (2) three redundant caller-side `conn.commit()`/`write_conn.commit()` calls (which could have masked a future service-level commit regression) were found and removed — both `TradeService.ingest_batch()` and `TradeService.rebuild_all_lifecycles()` already commit their own transaction internally via `_r5_write_transaction()`, and the suite's fresh-connection durability proof now depends solely on that; all tests were rerun after both corrections with identical results
  - 35/35 new focused tests passing (`_LifecycleCorpusAcceptanceTestCase`, `CorpusIngestionAndGlobalInvariantTests`, `CorpusLifecycleOracleTests`, `CorpusAmbiguityFlagTests`, `CorpusLifecycleIdempotencyTests`); 1181/1181 tests passing overall (1146 pre-existing + 35 new), 0 failures, 0 errors, passing on the first full-suite run with no Streamlit `AppTest` timeout
  - Exactly one file changed: `tests/test_lifecycle_corpus_acceptance.py` (1 file changed, 801 insertions(+), 0 deletions(-)); no production, schema, migration, or existing test file changed
  - Implementation commit `1cd09a1b29eb8e1cc9abd4dde4d865dea0b8adf4` ("Add real-corpus lifecycle acceptance tests") pushed to `origin/main` as a normal fast-forward (`33d77e1..1cd09a1`); local `main` and `origin/main` synchronized at that commit, 0 ahead / 0 behind. No new tag was created or moved; the existing `v0.1.0` tag remains unrelated and unchanged
  - Trader-performance calculations are not part of R6.6; Recovery Milestone R7 (trader-performance analytics) and R8 (trader-performance dashboard/UI) remain required before the Product Owner can evaluate every trader's performance in the completed application
- Recovery Milestone R6.7: Validated Lifecycle Event Read API — implemented, tested, reviewed, committed, and pushed (see docs/HANDOFFS/R6.7_validated_lifecycle_event_read_api.txt; implementation commit `8329b81f0fc0a06ddc1cd840de0a8f8dd59a0d54` — "Add validated lifecycle event read API"; documentation commit `6be3856604bd8bd158af6e94ecdad48e04d3b180` — "Update documentation for Recovery Milestone R6.7")
  - New `TradeService.list_trade_lifecycle_events(self, trade_lifecycle_id: int) -> list[dict]` — a thin, read-only delegation to the existing `database.repository.get_trade_lifecycle_events()`, preserving repository-provided (`sequence_index` ascending) order unchanged; validates and decodes every member's immutable `signal_snapshot` via the existing, completely unmodified private `_validate_and_decode_snapshot()` (the same validator `rebuild_all_lifecycles()`/`rebuild_lifecycles_for_raw_message_ids()` already use internally) — no second decoding/validation implementation exists; returns plain dicts containing exactly `id`, `trade_lifecycle_id`, `trade_signal_id`, `sequence_index`, `created_at`, `snapshot` (the decoded dict, never raw JSON text); returns `[]` when a generation has no members or does not exist; performs no write, opens no transaction, and calls neither `commit()` nor `rollback()`
  - `database/service.py` (additive only): the new method plus a documentation-only correction to `LifecycleSnapshotError`'s class docstring, so it accurately covers both lifecycle-rebuild-time and this new read-only-query-time use (during a rebuild, propagation still rolls back the owning rebuild transaction; during a read, no write was ever attempted, so there is nothing to roll back) — the exception's constructor and runtime behavior are completely unchanged
  - No repository, lifecycle-engine, schema, migration, parser, adapter, or UI change of any kind; `database/repository.py`, `database/lifecycle.py`, `database/models.py`, `database/schema.sql`, every file under `database/migrations/`, and `app/streamlit_app.py` are all unchanged
  - Exactly two files changed: `database/service.py`, `tests/test_service.py` (2 files changed, 376 insertions(+), 4 deletions(-))
  - New test class `TradeServiceLifecycleReadTests` in `tests/test_service.py` (19 new tests) covering repository-argument delegation, empty results, decoded (never raw) snapshot content, exact returned metadata keys, order preservation (fed deliberately out of numeric `sequence_index` order to prove no re-sort occurs), invalid/non-object JSON, a missing required snapshot field, a `trade_signal_id` identity mismatch, `ordering_key` validation (both canonical two- and three-element forms), and caller-owned-transaction preservation (a real file-backed SQLite database with an uncommitted probe write, proving the read neither commits nor rolls back the caller's pending work, verified via a second independent connection)
  - Quality-review correction made before this documentation: the initial `test_ordering_key_wrong_type_raises` test unintentionally passed its string argument through the same `SignalSnapshot`/`build_signal_snapshot_json()` helper the other ordering-key tests use, whose serializer stores `list(snapshot.ordering_key)` — silently turning the string into a 10-element character array (retesting "wrong length," not "wrong type"). Corrected to mutate an already-decoded snapshot dict directly and re-serialize with plain `json.dumps()`, so the persisted JSON genuinely contains a non-array `ordering_key` value; the separate wrong-length test was left unchanged, and the five other ordering-key tests (two-element mismatched raw_message_id, two-element mismatched trade_signal_id, three-element mismatched raw_message_id, three-element mismatched trade_signal_id, and three-element blank received_at) were reviewed and confirmed already correctly targeted
  - 1200/1200 tests passing (1181 pre-existing + 19 new), 0 failures, 0 errors; one initial full-suite run hit a single transient Streamlit `AppTest` timeout in the untouched `tests/test_app.py` (the same previously-documented transient pattern noted in the R6.6 handoff), confirmed unrelated via an isolated rerun (passed in 0.714s) and a clean confirming full-suite rerun (1200/1200)
  - No `Co-Authored-By` trailer exists on the implementation commit; no tag was created or moved; all 12 pre-existing unrelated untracked entries were confirmed present and untouched throughout
  - R7 (trader-performance analytics) was approved for planning and implementation after this milestone's own documentation pass was pushed; see the R7 entry immediately below. R8 (trader-performance dashboard/UI) remains unstarted, with no reviewed design, and requires its own separate Product Owner / Technical Architect (ChatGPT) approval before it begins
- Recovery Milestone R7: Trader-Performance Analytics — implemented, tested, reviewed, committed, and pushed (see docs/HANDOFFS/R7_trader_performance_analytics.txt; implementation commit `6d74a13c38914d0cb80c0be9f1236b291ccafb24` — "Implement trader-performance analytics"; documentation commit `909967741fce577e5c77ac3d40d51527df1b2dfa` — "Update documentation for Recovery Milestone R7")
  - New pure, database-independent analytics engine `database/analytics.py` (no `sqlite3` import): `compute_lifecycle_analytics()` computes one lifecycle generation's price-return outcome from already-decoded, already-ordered member events; `build_data_error_result()` builds a 'data_error' result for structurally broken evidence; `summarize_trader_performance()` reduces many results into one trader's performance summary. Price-return analytics only — no dollar P&L, contract-multiplier, or position-quantity calculation, and `trade_signals.stated_return_pct` is never read for any calculation
  - Fraction-weighted partial exits: uses `fractions.Fraction` (never Decimal) for each exit leg's `consumed_fraction`, since approved qualifier tokens (`1/3`, `1/6`) are non-terminating in base 10; `gross_price_return_pct` is the fraction-weighted sum across every exit leg, never the naive entry-to-final-price shortcut
  - Trade-direction classification from the opener's raw `action` verb only (`event_type` alone cannot distinguish long from short): `BTO`/`BUY`/`BOUGHT` classify long and are scored; `STO` classifies short and is excluded (`short_direction_not_scored`) — never assumed absent, never scored under the long-position return formula
  - Scale-in exclusion: any lifecycle containing an `ADD` event is excluded from return/outcome metrics (`scale_in_cost_basis_not_modeled`, no cost-basis model exists for a scaled-in position) while still counting normally toward status-count aggregates
  - All calculation performed at `decimal.localcontext()` precision 50 with `ROUND_HALF_EVEN`; every public percentage field is quantized to exactly six decimal places only at serialization, never before classification; every Decimal-valued result field is a plain string, never a float; median uses true division, never floor division
  - New unbounded (no `LIMIT`) repository reader `database.repository.get_all_current_trade_lifecycles()`, deliberately distinct from the pre-existing bounded (default `LIMIT 100`) display reader `list_current_trade_lifecycles()`, so an analytics-completeness query can never silently truncate — proven with a 101-lifecycle no-truncation test at both the repository and service layers
  - Three new public, read-only `TradeService` methods, none opening a transaction or calling `commit()`/`rollback()`: `get_trade_lifecycle_analytics()` (strict — raises `TradeLifecycleNotFoundError`, `LifecycleAnalyticsError`, or a propagated-unchanged `LifecycleSnapshotError` rather than ever substituting a placeholder result), `list_current_trade_lifecycle_analytics()` (tolerant — one corrupted lifecycle can never abort the call; surfaces as an explicit `data_error` result instead), and `list_trader_performance_summaries()` (tolerant; shares its per-lifecycle computation with the list method via one private helper, so no snapshot is ever decoded twice; never ranks by performance, `trader_id` ascending only)
  - No schema, migration, lifecycle-engine (`database/lifecycle.py`), `database/models.py`, parser, adapter, or UI change of any kind; `list_trade_lifecycle_events()` and `LifecycleSnapshotError` (R6.7) are unchanged
  - Exactly six files changed: `database/repository.py`, `database/service.py`, `database/analytics.py` (new), `tests/test_repository_recovery.py`, `tests/test_service.py`, `tests/test_analytics.py` (new) (6 files changed, 1996 insertions(+), 0 deletions(-)), committed in implementation commit `6d74a13c38914d0cb80c0be9f1236b291ccafb24`; documentation (this file and docs/HANDOFFS/R7_trader_performance_analytics.txt) committed separately via commit `909967741fce577e5c77ac3d40d51527df1b2dfa` (2 files changed, 561 insertions(+), 14 deletions(-))
  - 44 new focused tests: 24 in the new `tests/test_analytics.py` (pure-module coverage, no database involved), 6 in a new `GetAllCurrentTradeLifecyclesTests` class in `tests/test_repository_recovery.py`, and 14 in a new `TradeServiceAnalyticsTests` class in `tests/test_service.py` (real pipeline through a real temporary SQLite database, no mocking)
  - 1244/1244 tests passing (1200 pre-existing + 44 new), 0 failures, 0 errors; `git diff --check` passed with no output; one initial full-suite run hit a single transient, unrelated Streamlit `AppTest` timeout in the untouched `tests/test_app.py` (the same previously-documented transient pattern noted in the R6.6 and R6.7 handoffs), confirmed via an isolated rerun (passed in 0.884s) and a clean confirming full-suite rerun (1244/1244)
  - A pre-existing test-reporting question (whether the shared test helper `_build_closed_long_lifecycle()`'s calls used `symbol="IBM"` or `symbol="AVGO"`) was checked during verification and resolved as not a defect: the helper defaults to `"IBM"`; several call sites pass `symbol="AVGO"` explicitly to build a second, distinguishable lifecycle within the same test. No code change was made
  - Both commits pushed to `origin/main` in a single normal fast-forward push (`6be3856..9099677`); local `main` and `origin/main` synchronized at commit `909967741fce577e5c77ac3d40d51527df1b2dfa`, 0 ahead / 0 behind. No `Co-Authored-By` trailer on either commit; no tag was created or moved; the existing `v0.1.0` tag remains unrelated and unchanged — Recovery Milestone R7 is fully complete
  - Recovery Milestone R8 was split into R8a (core dashboard/UI) and R8b (ranking/sort selector, CSV export) after R7's own documentation pass was reviewed and pushed; R8a's own approved planning, implementation, quality review, and correction of three minor test-quality findings are recorded immediately below. R8b remains unstarted, with no reviewed design, and requires its own separate Product Owner / Technical Architect (ChatGPT) approval before it begins
- Recovery Milestone R8a: Trader-Performance Dashboard (Core) — implemented, tested, independently quality-reviewed, committed, and pushed (see docs/HANDOFFS/R8a_trader_performance_dashboard_core.txt; implementation commit `2fac9a210521ce150426520fdf2c505f782f4a4f` — "Implement trader-performance dashboard core"; documentation commit `98c4a830f6af141579055449a64d1d6121908502` — "Update documentation for Recovery Milestone R8a"). Both commits are present on `origin/main`; local `main` and `origin/main` are synchronized at `98c4a830f6af141579055449a64d1d6121908502`, 0 ahead / 0 behind — Milestone R8a is fully complete.
  - Adds the third sidebar workflow, `"Trader Performance"`, to the existing `st.sidebar.radio("Workflow", [...])` control - the one approved change to that line; Manual Message Entry, Review Signals, and Correct Signal remain behaviorally unchanged and are fully regression-tested (not "byte-for-byte unchanged," since the options list itself necessarily grew by one entry)
  - Trader Summary: one row per unique `trader_id` (never merged by `trader_name`), rendered exactly in the `trader_id`-ascending order returned by `TradeService.list_trader_performance_summaries()` - no ranking, sorting control, or re-derivation of any kind
  - Trader drill-down (`TradeService.list_current_trade_lifecycle_analytics(trader_id=...)`): independently optional and combinable status filter, outcome filter, and exact-uppercase (never substring) symbol filter; every lifecycle-level metric, ambiguity flag, and exclusion reason displayed verbatim from the service's own result
  - Lifecycle-detail panel (a second, lifecycle-scoped selectbox): exit-leg evidence (event type, consumed fraction, exit price, sequence index, in the service's own sequence_index-ascending order) and the verbatim `analytics_error_detail` text - the only place that field is ever shown; the summary table and drill-down table expose only a `⚠`/count data-error indicator, never the detail text itself
  - Duplicate-named traders always render as `{trader_name} (ID {trader_id})`, both in the summary table and the trader selector, so two traders sharing a display name are never conflated - proven end-to-end with two real, distinct `trader_id`s sharing the name "TC" in the integration suite
  - Stale-selection safety: a previously selected trader or lifecycle that no longer appears (a trader change, a filter narrowing it out, or the underlying data changing between reruns) is cleared via the same `session_state`-pop-before-render pattern already established for `correction_signal_id` in Review Signals - proven to never raise `StreamlitAPIException`
  - Missing-database and failure handling mirrors Review Signals exactly: `Path(resolve_database_path()).exists()` checked before any access - never `initialize_database()`, never a connection opened, when the database does not yet exist; summary-load and drill-down-load failures each show a fixed, sanitized message via the existing `log_operation_failure()` contract, never raw exception text
  - Uses only the two existing Recovery Milestone R7 service methods - `TradeService.list_trader_performance_summaries()` and `TradeService.list_current_trade_lifecycle_analytics(trader_id=...)` - no other `TradeService` method is called from this workflow, and no new backend method was added
  - `app/dashboard_formatting.py` (new, pure): eleven presentation-only helper functions (`format_optional_value`, `format_percent`, `format_data_error_count`, `build_trader_label`, `build_summary_display_row(s)`, `filter_lifecycle_results`, `build_lifecycle_display_row(s)`, `build_exit_leg_display_rows`, `build_lifecycle_detail`) - no `streamlit`, `sqlite3`, `database.repository`, `database.service`, or `database.analytics` import; no calculation, parsing, rounding, or ranking of any R7 analytics value, confirmed by direct inspection
  - No schema, migration, `database/repository.py`, `database/service.py`, `database/lifecycle.py`, `database/models.py`, `app/parser.py`, `app/discord_adapter.py`, `requirements.txt`, or dependency change of any kind; no ranking control, CSV/Excel export, chart, or minimum-sample-size logic - all explicitly deferred to Recovery Milestone R8b
  - Exactly five files changed - two modified, three new: `app/streamlit_app.py` (150 insertions(+), 1 deletion(-)), `tests/test_app.py` (477 insertions(+), 2 deletions(-)), `app/dashboard_formatting.py` (new, 184 insertions(+)), `tests/test_dashboard_formatting.py` (new, 454 insertions(+)), `tests/test_dashboard_integration.py` (new, 276 insertions(+)) — 5 files changed, 1541 insertions(+), 3 deletions(-)
  - 78 new tests: 43 in the new `tests/test_dashboard_formatting.py` (pure-module coverage, no database or Streamlit involved), 27 in a new `TraderPerformanceWorkflowTests` class in `tests/test_app.py` (AppTest component coverage, `TradeService` mocked), and 8 in the new `tests/test_dashboard_integration.py` (real pipeline through a real temporary SQLite database - real `rebuild_all_lifecycles()`, real `database.analytics` computation, no mocking)
  - 1322/1322 tests passing (1244 pre-existing + 78 new), 0 failures, 0 errors; `git diff --check` passed with no output; the full-suite run required no transient rerun - a single clean pass, unlike the transient Streamlit `AppTest` timeout previously documented in the R6.6/R6.7/R7 handoffs
  - An independent quality-review pass identified three MINOR test-quality findings (no BLOCKER or MAJOR finding, and no production-behavior impact from any of them), all corrected before this documentation: (1) `test_combined_filters_apply_together` originally could pass even if the status filter were silently broken, since the outcome filter alone already produced the same result under the shared fixture - corrected to use a local four-row fixture where only the intersection of both filters yields the expected single row; (2) `test_summary_order_matches_service_return_order` and `test_duplicate_trader_names_render_distinct_labels` were byte-identical in body - the former corrected to use a local fixture returned in deliberately non-ascending `trader_id` order, so it can only pass if the UI genuinely preserves the service's own order; (3) `test_ambiguity_and_exclusion_reasons_visible_in_drilldown` asserted only on Exclusion Reasons despite its name - renamed to `test_exclusion_reasons_visible_in_drilldown`, and a new `test_ambiguity_flags_visible_in_drilldown` test was added with a local non-empty `lifecycle_ambiguity_flags` fixture, closing the only coverage gap where the real `st.dataframe()` rendering of that column had never been exercised end-to-end (the `tests/test_app.py` statistics above already reflect this correction)
  - Recovery Milestone R8b (ranking/sort selector, CSV export, and any other polish explicitly deferred from R8a) is now implemented, tested, and independently quality-reviewed locally — see the dedicated Completed entry immediately below
- Recovery Milestone R8b: Trader-Performance Dashboard — Ranking, Minimum-Sample Threshold, and CSV Export — implemented, tested, independently quality-reviewed, committed, and pushed (see docs/HANDOFFS/R8b_trader_ranking_and_csv_export.txt; implementation commit `012b075aee9907c1d9b5941a462f90a0bc7d99aa` — "Implement trader ranking and CSV export"; documentation commit `256bd3ee8300b56932183b6247f86940e6086094` — "Update documentation for Recovery Milestone R8b"). **This is the final sub-milestone of the Trader-Performance Dashboard module (Recovery Milestone R8) — the module (R8a + R8b) is fully complete.**
  - Trader ranking and comparison added to the existing Trader Summary table: a user-selectable "Rank traders by" metric (the ten approved metrics: Average Return, Median Return, Win Rate, Loss Rate, Avg Winner Return, Avg Loser Return, Eligible Lifecycles, Total Lifecycles, Trader Name, Trader ID) and an explicit "Sort direction" selectbox (Descending/Ascending); default ranking is Average Return, Descending
  - New minimum eligible-lifecycle threshold control ("Minimum eligible lifecycles", default `3`): a trader qualifies when `eligible_lifecycle_count >= threshold` (never recalculated — read verbatim from the existing R7 service result); qualifying traders are always ranked before below-threshold traders; below-threshold traders always remain fully visible, never hidden (no hide-below-threshold control exists); a new "Meets Minimum Sample" column shows `"Yes"` or a clear `"No (n < threshold)"` indicator
  - Deterministic ranking rules: percentage/return metrics compare as `Decimal` (never float or a formatted string); lifecycle-count and trader-ID metrics compare as `int`; trader-name sorting is case-insensitive; a `None` metric value (R7's own zero-denominator contract) always sorts last in either direction; the final tie-break is always `trader_id` ascending, unconditionally of direction; ranking never mutates the service-returned list and is never delegated to `st.dataframe`'s own interactive sort
  - The trader selector's options and their order now follow the identical ranked sequence the summary table renders from — the highest-ranked qualifying trader is the first (default-selected) option on initial load; changing any ranking control never clears a still-present selected trader
  - New CSV export: "Download Trader Summary CSV" (`trader_performance_summary.csv`, current ranked order and threshold state, "Trader Name"/"Trader ID" as separate columns, "Meets Minimum Sample" included, fixed documented column order) and "Download Lifecycle Drill-down CSV" (`trader_lifecycle_drilldown_{trader_id}.csv`, current trader and current status/outcome/symbol filters only, same order as the displayed table, `analytics_error_detail` never exported); both render only when their corresponding table has rows and are absent in every missing-database/empty/failure/no-match state; generation uses only the Python standard library (`csv` + `io.StringIO`, never pandas), with deterministic `\r\n` line endings and correct quoting; an unexpected CSV-generation failure shows a fixed sanitized message via the existing `log_operation_failure()` pattern, never raw exception text
  - No UI-side analytics recomputation of any kind — ranking and CSV export operate only on already-computed R7 values, exactly like R8a's own display formatting
  - No schema, migration, `database/repository.py`, `database/service.py`, `database/lifecycle.py`, `database/models.py`, `app/parser.py`, `app/discord_adapter.py`, `requirements.txt`, or dependency change; no new implementation file (R8b extends R8a's existing `app/dashboard_formatting.py` and test files); no chart, no Excel export, no dollar P&L, no position sizing, no Bulk Channel Import UI work
  - Exactly five files changed, all modified: `app/dashboard_formatting.py` (252 insertions(+), 8 deletions(-)), `app/streamlit_app.py` (97 insertions(+), 3 deletions(-)), `tests/test_app.py` (369 insertions(+), 23 deletions(-)), `tests/test_dashboard_formatting.py` (411 insertions(+), 13 deletions(-)), `tests/test_dashboard_integration.py` (253 insertions(+), 7 deletions(-)) — 5 files changed, 1382 insertions(+), 54 deletions(-)
  - 68 new tests: 41 in `tests/test_dashboard_formatting.py` (43 -> 84), 19 in `tests/test_app.py`'s `TraderPerformanceWorkflowTests` (27 -> 46), 8 in the new `RankingAndExportIntegrationTests` class in `tests/test_dashboard_integration.py` (8 -> 16, real SQLite, no mocking); `tests/test_app.py` complete: 148/148 passing
  - 1390/1390 tests passing (1322 pre-existing + 68 new), 0 failures, 0 errors; `git diff --check` passed with no output (two harmless CRLF-normalization warnings only). Test execution history: the implementation pass hit one transient Streamlit `AppTest` timeout (the same previously-documented pattern from R6.6/R6.7/R7/R8a), confirmed unrelated via an isolated rerun (passed in 0.875s), a clean class rerun (46/46), and a clean full-suite rerun (1390/1390); the subsequent independent quality-review pass then reran every required suite cleanly with no transient rerun at any point
  - An independent quality-review pass found no BLOCKER and no MAJOR finding. Five MINOR test-quality findings were identified and explicitly accepted by the Product Owner without correction (all remain as implemented; see docs/HANDOFFS/R8b_trader_ranking_and_csv_export.txt Section 12 for full detail): two AppTest-level "exact content" CSV tests prove wiring rather than independently verifying content (content correctness is otherwise proven with literal values elsewhere); one pure-module test asserts tautological delegation-equality rather than a literal value; one pure-module test compares two formatting functions against each other rather than a literal value; one coverage gap where an ascending-direction/below-threshold-tier combination and a threshold-change/selector-reorder combination are each proven only in pieces, not chained (low risk, since the ranking comparator uses one shared, tier-agnostic code path); and pre-existing (not R8b-introduced) brittle positional widget-index tests that could be replaced with Streamlit `AppTest`'s supported key-based lookup. Two additional non-actionable observations were also noted. None of the five findings or two observations affects production behavior, and no further correction was required before this documentation
  - Implementation commit `012b075aee9907c1d9b5941a462f90a0bc7d99aa` and documentation commit `256bd3ee8300b56932183b6247f86940e6086094` are both on `origin/main`; local `main` and `origin/main` were synchronized at `256bd3ee8300b56932183b6247f86940e6086094`, 0 ahead / 0 behind, once both landed — Recovery Milestone R8b, and therefore the entire Trader-Performance Dashboard module (Recovery Milestone R8), is fully complete
- Recovery Milestone R9a: Bulk Channel Import Backend Read Contracts — implemented, tested, committed, and pushed (implementation commit `387d2aa1166bd74527da7565f6c3ec4a1367f451` — "Implement channel import backend contracts"). No dedicated `docs/HANDOFFS/` entry or separate documentation commit was made for R9a at the time; it is documented here retroactively, together with Recovery Milestone R9b, in this same documentation pass.
  - New migration `database/migrations/0008_channel_import_operations.sql`: adds `channel_import_operations`, one row per successfully completed confirmed Bulk Channel Import operation for one channel (a failed/rolled-back attempt is never persisted here — the row's mere existence is its own success signal); CHECK constraints enforce `processed_count >= 15` (the approved Bulk Channel Import minimum-batch-size floor, enforced at the database level, not only in service-layer validation), `stored_count + duplicate_count = processed_count`, `unrecognized_count`/`failed_count <= stored_count`, and `stored_count = 0 <=> import_batch_id IS NULL`
  - `database/models.py` (additive): four new frozen dataclasses — `ChannelImportOperation` (mirrors the new table), `ChannelExternalIdAvailability`, `ChannelImportDuplicatePrediction`, `ChannelImportChannelSummary` (a channel's combined selection summary: the channel itself, its current `ChannelCheckpoint`, and its latest `ChannelImportOperation`)
  - `database/repository.py` (additive): `create_channel()`, `get_channel_by_external_id()`, `get_channel_by_id()`, `list_channels()`, `create_channel_import_operation()`, `get_latest_channel_import_operation()`; `UNSPECIFIED_CHANNEL_EXTERNAL_ID` and `compute_content_hash()` promoted from private/repository-only to public, shared exactly with `database/service.py`
  - `database/service.py` (additive): four new strictly read-only `TradeService` public methods — `list_bulk_import_channels()`, `get_bulk_import_channel_summary()`, `check_new_channel_external_id_availability()`, `predict_channel_import_duplicate_statuses()` — none opens a transaction, writes, or calls `get_or_create_source()`/`get_or_create_channel()`; every check this milestone performs is explicitly advisory, since the confirmed, authoritative write path was deliberately deferred to Recovery Milestone R9b (its own atomic ingest-plus-lifecycle-rebuild transaction, `_ingest_batch_no_commit()` and `import_channel_batch_with_lifecycle_rebuild()`, was not implemented in R9a)
  - No schema change to any pre-existing table, no lifecycle-engine, parser, adapter, or UI change of any kind
  - Exactly eight files changed: `database/migrations/0008_channel_import_operations.sql` (new), `database/models.py`, `database/repository.py`, `database/service.py`, `tests/test_bulk_channel_import_service.py` (new), `tests/test_migrations.py` (new), `tests/test_models.py`, `tests/test_repository_recovery.py` (8 files changed, 2068 insertions(+), 7 deletions(-))
  - 1491/1491 tests passing (1390 pre-existing + 101 new), 0 failures, 0 errors
  - Backend-only: no Bulk Channel Import UI work of any kind — that is Recovery Milestone R9c's own explicit scope. Local `main` and `origin/main` were synchronized at `387d2aa1166bd74527da7565f6c3ec4a1367f451`, 0 ahead / 0 behind, once pushed
- Recovery Milestone R9b: Atomic Bulk Channel Import — implemented, tested, reviewed, committed, and pushed (see docs/HANDOFFS/R9b_atomic_bulk_channel_import_handoff.txt; implementation commit `e9b49df9c00d6d7e9b5d088711932a0d3e22bf6b` — "Implement atomic bulk channel import")
  - New `database/models.py` dataclass `AtomicChannelImportResult` (frozen, exactly four fields in this order: `channel`, `batch_result`, `lifecycle_result`, `operation`) — the result of one successful atomic import, only ever returned after every write of the call has already committed
  - New `database/service.py` exception `ChannelExternalIdCollisionError` (a `ValueError` subclass) — raised in create mode when a candidate new channel external id already belongs to an existing channel, whether discovered by an up-front check or only after a confirmed `create_channel()` unique-constraint race (a re-query after `sqlite3.IntegrityError` confirms a real, existing colliding row before translating; an unconfirmed `IntegrityError` propagates unchanged, never mistranslated)
  - New private `TradeService._ingest_batch_no_commit()`: the exact former transaction body of the pre-existing Recovery Milestone R5 `ingest_batch()`, mechanically extracted (never begins/commits/rolls back a transaction, never re-segments, never resolves source/channel) so both `ingest_batch()` (now a thin wrapper delegating to it, with its own public contract completely unchanged and fully regression-tested) and the new atomic method share exactly one implementation of batch ingestion
  - New private `TradeService._collect_newly_stored_raw_message_ids()`: collects every newly-stored `raw_message_id` from a batch result, guarding two internal invariants with a fixed `RuntimeError` each (a "stored" outcome with no `raw_message_id`; a collected count not matching `stored_count`) — defensive validation that, if ever tripped, still rolls back the entire surrounding transaction
  - New public `TradeService.import_channel_batch_with_lifecycle_rebuild()` — the atomic operation. Accepts a previously previewed batch of at least 15 exact `SegmentedMessage` objects (the 15-message minimum is enforced only here, never by `ingest_batch()`/`_ingest_batch_no_commit()`) and an explicit `channel_mode: Literal["existing", "create"]`, using the exact pre-segmented sequence supplied — it is never re-segmented
  - Existing mode: `existing_channel_id` required (a strict positive, non-boolean integer); `new_channel_external_id`/`new_channel_name` must both be `None` (rejected as contradictory otherwise); the source is looked up via `get_source_by_name()` only — never created — and a missing source is a fixed rejection; the selected channel must already exist, must belong to the resolved source, and must not be the reserved `UNSPECIFIED_CHANNEL_EXTERNAL_ID` sentinel channel
  - Create mode: `existing_channel_id` must be `None` (rejected as contradictory otherwise); `new_channel_external_id` required, non-blank, and must not be the reserved sentinel; `new_channel_name` remains optional with `create_channel()`'s own existing behavior preserved exactly; the source is resolved via `get_or_create_source()` (may create a new `sources` row as part of the same atomic transaction); the channel is created via `create_channel()` directly — `get_or_create_channel()` is never called in this mode — after an up-front collision check, with `ChannelExternalIdCollisionError` translation only after a confirmed race
  - Targeted lifecycle rebuild: only the lifecycle keys affected by the newly stored raw-message ids are rebuilt, via the existing private `_rebuild_lifecycles_for_raw_message_ids_no_commit()` — never a full-database sweep
  - Duplicate-only success: when every segmented message is already present, no `import_batches` row is created, no lifecycle rebuild is attempted at all (an empty, zero-valued lifecycle result equivalent to `_RebuildCounters().to_result()` is used directly, and the targeted-rebuild helper is proven never called), and a successful `channel_import_operations` row is still recorded with `stored_count=0`/`import_batch_id=None`, becoming the channel's latest successful operation
  - Full atomicity: channel/source creation (create mode), ingestion, the targeted lifecycle rebuild, and the recorded `channel_import_operations` row all happen inside exactly one existing `_r5_write_transaction()` — any failure anywhere in that sequence rolls back every one of those writes together; a failed or rolled-back attempt is never recorded as an operation row
  - Exactly four files changed: `database/models.py`, `database/service.py`, `tests/test_bulk_channel_import_service.py`, `tests/test_models.py` (4 files changed, 1547 insertions(+), 85 deletions(-))
  - 1535/1535 tests passing (1491 pre-existing + 44 new: new `AtomicImportValidationTests`, `AtomicImportExistingModeTests`, `AtomicImportCreateModeTests`, `AtomicImportMixedOutcomeTests`, `AtomicImportDuplicateOnlyTests`, `AtomicImportOperationRecordTests`, `AtomicImportLifecycleRebuildTests`, `AtomicImportRollbackTests`, and `CollectNewlyStoredRawMessageIdsTests` classes in `tests/test_bulk_channel_import_service.py`, plus a new `AtomicChannelImportResultModelTests` class in `tests/test_models.py`), 0 failures, 0 errors; the complete pre-existing regression suite (`tests/test_service.py`, `tests/test_service_lifecycle.py`, `tests/test_repository_recovery.py` — 451 tests) required no change and passed unmodified, proving `ingest_batch()`'s own extraction preserved its public contract exactly
  - Went through two review passes before commit: an initial architecture correction (source resolution now branches by mode — existing mode never creates a source; create mode may, with rollback proof) and a subsequent test-hardening correction pass (an exact ordered-sequence proof replacing a set-only comparison, a ten-table `_atomic_write_counts()` rollback helper applied to every forced-failure test, an explicit duplicate-only proof that the targeted-rebuild helper is never called, a missing create-mode required-id test, and three substring assertions tightened to exact fixed messages) — both fully detailed in docs/HANDOFFS/R9b_atomic_bulk_channel_import_handoff.txt
  - Implementation commit `e9b49df9c00d6d7e9b5d088711932a0d3e22bf6b` pushed to `origin/main` as a normal fast-forward (`387d2aa..e9b49df`); local `main` and `origin/main` synchronized at that commit, 0 ahead / 0 behind. No `Co-Authored-By` trailer exists on the commit; no tag was created or moved; all 12 pre-existing unrelated untracked entries were confirmed present and untouched throughout
  - Backend-only: no Bulk Channel Import UI work of any kind — the next and final sub-milestone of the Bulk Channel Import module is Recovery Milestone R9c (Bulk Channel Import UI), which has no reviewed design yet, has not started, and requires its own separate scoping/design planning pass and explicit Product Owner / Technical Architect (ChatGPT) approval before any implementation work begins. Recovery Milestone R9 (Bulk Channel Import) as a whole is **not** yet complete — it becomes complete only once R9c is implemented, tested, reviewed, committed, and pushed

# Current Focus
The manual v0.1.0 track (Phase 2D, through Milestone 2D.7) is complete and released: the annotated tag `v0.1.0` exists and was pushed, pointing to commit `6e9350b5866c594ee4998f938459ac3db6056c1e`; the original manual v0.1.0 workflow remains fully usable exactly as documented in the Milestone entries above. Work is now in the Recovery Phase, layered on top of that released version. Recovery Milestones R1 through R5 are complete and pushed to `origin/main`. Recovery Milestones R6.1 through R6.4 (lifecycle schema foundation, the pure lifecycle-matching engine, the lifecycle repository layer, and lifecycle rebuild orchestration) are complete and pushed. Recovery Milestone R6.5a — the lifecycle-safe `TradeService.correct_trade_signal()` service contract — is complete and pushed at commit `2018f2bb97a4c695c13cfd83f6245a7cd85aa57e` (see docs/HANDOFFS/R6.5a_lifecycle_safe_trade_signal_correction.txt). Recovery Milestone R6.5b — migrating the shipped "Correct Signal" UI to that lifecycle-safe `correct_trade_signal()` API — is complete and pushed at commit `bd911ac04c12e45e3501202f45124d7233ebe723` (see docs/HANDOFFS/R6.5b_lifecycle_safe_correction_ui_integration.txt). Recovery Milestone R6.6 — Real-Corpus Lifecycle Acceptance — is complete, tested, reviewed, committed, and pushed at commit `1cd09a1b29eb8e1cc9abd4dde4d865dea0b8adf4` ("Add real-corpus lifecycle acceptance tests"; see docs/HANDOFFS/R6.6_real_corpus_acceptance.txt). Recovery Milestone R6.7 — Validated Lifecycle Event Read API — is also complete, tested, reviewed, committed, and pushed: implementation commit `8329b81f0fc0a06ddc1cd840de0a8f8dd59a0d54` ("Add validated lifecycle event read API") and documentation commit `6be3856604bd8bd158af6e94ecdad48e04d3b180` ("Update documentation for Recovery Milestone R6.7"; see docs/HANDOFFS/R6.7_validated_lifecycle_event_read_api.txt), adding `TradeService.list_trade_lifecycle_events()` - a validated, decoded read API for persisted lifecycle-event evidence, with no repository, lifecycle-engine, schema, or UI change. Local `main` and `origin/main` are synchronized at commit `6be3856604bd8bd158af6e94ecdad48e04d3b180`, 0 ahead / 0 behind. No new tag was created; the existing `v0.1.0` tag remains unrelated and unchanged.

Recovery Milestone R7 — Trader-Performance Analytics — is now also complete, tested, reviewed, committed, and pushed: implementation commit `6d74a13c38914d0cb80c0be9f1236b291ccafb24` ("Implement trader-performance analytics") and documentation commit `909967741fce577e5c77ac3d40d51527df1b2dfa` ("Update documentation for Recovery Milestone R7"; see docs/HANDOFFS/R7_trader_performance_analytics.txt), pushed together to `origin/main` as a single normal fast-forward (`6be3856..9099677`). It adds a pure `database/analytics.py` price-return analytics engine (fraction-weighted partial exits, scale-in and short-position exclusion, Decimal-precision-50/`ROUND_HALF_EVEN` calculation quantized to six places only at serialization), a new unbounded `database.repository.get_all_current_trade_lifecycles()` reader, and three new read-only `TradeService` methods - one strict (`get_trade_lifecycle_analytics()`) and two tolerant (`list_current_trade_lifecycle_analytics()`, `list_trader_performance_summaries()`) - with no schema, migration, lifecycle-engine, models, or UI change of any kind. Local `main` and `origin/main` are synchronized at commit `909967741fce577e5c77ac3d40d51527df1b2dfa`, 0 ahead / 0 behind. No new tag was created; the existing `v0.1.0` tag remains unrelated and unchanged.

Recovery Milestone R8a — Trader-Performance Dashboard (Core) — is now complete, tested, independently quality-reviewed, committed, and pushed: implementation commit `2fac9a210521ce150426520fdf2c505f782f4a4f` ("Implement trader-performance dashboard core") and documentation commit `98c4a830f6af141579055449a64d1d6121908502` ("Update documentation for Recovery Milestone R8a"; see docs/HANDOFFS/R8a_trader_performance_dashboard_core.txt), both present on `origin/main`. It adds the third sidebar workflow (`Trader Performance`), a trader summary table, a trader drill-down with independently combinable status/outcome/exact-uppercase-symbol filters, lifecycle-level metrics, ambiguity flags, and exclusion reasons, a lifecycle-detail panel with exit-leg evidence and the verbatim `analytics_error_detail` (shown only there), duplicate-trader-name separation by `trader_id`, and stale-selection safety - consuming only the two existing R7 service methods, with no UI-side analytics recalculation and no schema/migration/repository/service/lifecycle-engine/model/parser/adapter/analytics-calculation/requirement/dependency change. Local `main` and `origin/main` are synchronized at commit `98c4a830f6af141579055449a64d1d6121908502`, 0 ahead / 0 behind.

Recovery Milestone R8b — Trader-Performance Dashboard: Ranking, Minimum-Sample Threshold, and CSV Export — is now complete, tested, independently quality-reviewed, committed, and pushed: implementation commit `012b075aee9907c1d9b5941a462f90a0bc7d99aa` ("Implement trader ranking and CSV export") and documentation commit `256bd3ee8300b56932183b6247f86940e6086094` ("Update documentation for Recovery Milestone R8b"; see docs/HANDOFFS/R8b_trader_ranking_and_csv_export.txt), both on `origin/main`. It is the final sub-milestone of the Trader-Performance Dashboard module (Recovery Milestone R8, split into R8a and R8b), which is now fully complete. It adds trader ranking and comparison to the existing summary table (a selectable "Rank traders by" metric across the ten approved metrics, an explicit "Sort direction" selectbox, default Average Return descending), a minimum eligible-lifecycle threshold (default `3`, qualifying traders always ranked before below-threshold traders, below-threshold traders always remaining visible with a "Meets Minimum Sample" indicator), a trader selector that follows the ranked summary order, and deterministic CSV export of both the ranked Trader Summary table and the currently filtered Lifecycle Drill-down table (separate Trader Name/Trader ID CSV columns, Python-standard-library CSV generation only, never pandas) - with no UI-side analytics recomputation and no schema/migration/repository/service/lifecycle-engine/model/parser/adapter/analytics-calculation/new-dependency change.

Recovery Milestone R9a — Bulk Channel Import Backend Read Contracts — is complete, tested, committed, and pushed: implementation commit `387d2aa1166bd74527da7565f6c3ec4a1367f451` ("Implement channel import backend contracts"), documented retroactively together with R9b in this documentation pass (no dedicated docs/HANDOFFS/ entry exists for R9a alone). It adds a new migration (`database/migrations/0008_channel_import_operations.sql`, the `channel_import_operations` table), four new models (`ChannelImportOperation`, `ChannelExternalIdAvailability`, `ChannelImportDuplicatePrediction`, `ChannelImportChannelSummary`), repository additions (`create_channel()`, `get_channel_by_external_id()`, `get_channel_by_id()`, `list_channels()`, `create_channel_import_operation()`, `get_latest_channel_import_operation()`, plus `UNSPECIFIED_CHANNEL_EXTERNAL_ID`/`compute_content_hash()` promoted to public), and four new strictly read-only, advisory `TradeService` methods (`list_bulk_import_channels()`, `get_bulk_import_channel_summary()`, `check_new_channel_external_id_availability()`, `predict_channel_import_duplicate_statuses()`) - the confirmed, authoritative write path was deliberately deferred to R9b. Local `main` and `origin/main` were synchronized at `387d2aa1166bd74527da7565f6c3ec4a1367f451`, 0 ahead / 0 behind, once pushed.

Recovery Milestone R9b — Atomic Bulk Channel Import — is now complete, tested, reviewed, committed, and pushed: implementation commit `e9b49df9c00d6d7e9b5d088711932a0d3e22bf6b` ("Implement atomic bulk channel import"; see docs/HANDOFFS/R9b_atomic_bulk_channel_import_handoff.txt), pushed to `origin/main` as a normal fast-forward (`387d2aa..e9b49df`). It adds the atomic public `TradeService.import_channel_batch_with_lifecycle_rebuild()` - a new `AtomicChannelImportResult` model, a new `ChannelExternalIdCollisionError` exception, a shared `_ingest_batch_no_commit()` extracted from the pre-existing `ingest_batch()` (whose own public contract is unchanged), strict existing/create channel-mode validation, a 15-message minimum, exact pre-segmented-sequence use (never re-segmented), direct `create_channel()` use in create mode with confirmed-race-only collision handling, a targeted lifecycle rebuild scoped only to newly stored messages, duplicate-only success with no lifecycle rebuild attempted at all, one atomic transaction covering source/channel creation, ingestion, the lifecycle rebuild, and the recorded `channel_import_operations` row, and complete rollback of every one of those writes on any failure - with no schema, migration, `app/*.py`, or other file changed outside the four listed above. Local `main` and `origin/main` are synchronized at commit `e9b49df9c00d6d7e9b5d088711932a0d3e22bf6b`, 0 ahead / 0 behind. No new tag was created; the existing `v0.1.0` tag remains unrelated and unchanged.

# Next Milestone
Recovery Milestone R9c — Bulk Channel Import UI — is the next and final sub-milestone of the Bulk Channel Import module (Recovery Milestone R9, R9a + R9b + R9c). It has no reviewed design yet, has not started, and requires its own separate scoping/design planning pass and explicit Product Owner / Technical Architect (ChatGPT) approval before any implementation begins, in a new ChatGPT session. Do not start R9c automatically.

R9c must, at minimum: let the user paste at least 15 Discord messages in one batch; let the user select an existing channel or create a new one; preview the batch's segmentation before import; show duplicate prediction for the previewed messages; show per-channel checkpoint/resume information; confirm and run the atomic import (`TradeService.import_channel_batch_with_lifecycle_rebuild()`, Recovery Milestone R9b); display the just-completed operation's counts; refresh the Trader-Performance Dashboard (Recovery Milestone R8) so newly imported trades are reflected; and preserve the completed result on screen until the user explicitly starts a new batch (a "Start Next Batch" control), rather than clearing it automatically. Recovery Milestone R9 as a whole is **not** yet complete - it becomes complete only once R9c is implemented, tested, reviewed, committed, and pushed.

# Known Issues
- (Resolved by Recovery Milestones R3-R4) Date/time extraction and resolution are now implemented — see docs/HANDOFFS/R3_extractor_grammar_extension.txt and docs/HANDOFFS/R4_date_and_time_resolution.txt

# Documentation

Architecture:
FROZEN

Database Design:
FROZEN (docs/DATABASE_DESIGN_V1.md unchanged)

Implementation Roadmap:
Extended through Milestone 2D.7 (Phase 2D approved; commit 1ed88d3628823ce231f76d9ad9e4f5a46b953a64); Milestone 2D.7 was the final milestone under Phase 2D, and `v0.1.0` has since been released (see Release Status below). `docs/IMPLEMENTATION_ROADMAP.md` itself has not been re-opened or amended for the Recovery track — Recovery Milestones R1-R5 are not scoped there under any Phase 2E heading. Whether/how to fold the Recovery track into that roadmap document remains an open documentation-workflow question for the Product Owner/Technical Architect, not decided by this update.

Current Phase:
Recovery Phase (the Phase 2D / v0.1.0 manual-entry track is complete and released; Recovery work now proceeds on top of it)

Current Milestone:
Recovery Milestone R9b — Atomic Bulk Channel Import — implemented, tested, reviewed, committed, and pushed (see docs/HANDOFFS/R9b_atomic_bulk_channel_import_handoff.txt). Recovery Milestone R9a (Bulk Channel Import Backend Read Contracts) is itself already complete and pushed. Recovery Milestone R9c (Bulk Channel Import UI) is the next and final sub-milestone of the Bulk Channel Import module (Recovery Milestone R9) and has not started (see Next Milestone above and Next Action below).

Git Synchronization:
Local main: e9b49df9c00d6d7e9b5d088711932a0d3e22bf6b ("Implement atomic bulk channel import"). origin/main: e9b49df9c00d6d7e9b5d088711932a0d3e22bf6b (same commit). Ahead 0 / behind 0. This is the accurate, current pushed baseline - Recovery Milestone R9b is fully complete and pushed at this commit, as a normal fast-forward from `387d2aa1166bd74527da7565f6c3ec4a1367f451` (Recovery Milestone R9a's own implementation commit, "Implement channel import backend contracts"). This documentation update (this file and the new docs/HANDOFFS/R9b_atomic_bulk_channel_import_handoff.txt) exists only as uncommitted working-tree changes on top of this commit - nothing has been staged.

Release Status:
`v0.1.0` exists and was pushed (annotated tag `v0.1.0`, pointing to commit `6e9350b5866c594ee4998f938459ac3db6056c1e`). The original manual v0.1.0 release remains usable. No new tag was created for R6.5a, R6.7, R7, R8a, R8b, R9a, R9b, or any other Recovery-track work; `v0.1.0` remains unrelated and unchanged.

Recovery Status:
R1 complete and pushed. R2 complete and pushed. R3 complete and pushed. R4 complete and pushed. R5 complete and pushed. R6.1 complete and pushed. R6.2 complete and pushed. R6.3 complete and pushed. R6.4 complete and pushed. R6.5a complete and pushed. R6.5b complete and pushed. R6.6 complete and pushed. R6.7 complete and pushed. R7 complete and pushed. Recovery Milestone R8a — Trader-Performance Dashboard (Core) — complete and pushed. Recovery Milestone R8b — Trader-Performance Dashboard: Ranking, Minimum-Sample Threshold, and CSV Export — complete and pushed; the entire Trader-Performance Dashboard module (Recovery Milestone R8, R8a+R8b) is fully complete. Recovery Milestone R9a — Bulk Channel Import Backend Read Contracts — complete and pushed. Recovery Milestone R9b — Atomic Bulk Channel Import — complete and pushed. Recovery Milestone R9c — Bulk Channel Import UI — the final sub-milestone of the Bulk Channel Import module (Recovery Milestone R9) — remains unstarted; **Recovery Milestone R9 as a whole is not yet complete.**

Test Suite:
1535/1535 tests passing (0 failures, 0 errors; 1491 pre-existing + 44 new focused R9b tests, all currently uncommitted working-tree documentation changes only - no test or production code changed in this documentation pass)

User-Facing Readiness:
The existing manual v0.1.0 workflow remains usable. R5 backend ingestion, batch import, reprocessing, supersession, and checkpoints are implemented. R6.1-R6.4 backend lifecycle linking, R6.5a's lifecycle-safe correction service, and R6.5b's UI migration to that service are all implemented. R6.6 has proven, through real-corpus acceptance tests against the actual 68-message Discord corpus, that the complete R1-R6.5 pipeline correctly ingests, segments, extracts, date/time-resolves, and links every alert into the exact lifecycle outcome a human can verify by reading the corpus — R6.6 is test-only and changes no application behavior. The Product Owner can now correct a trade signal through the Review Signals UI using the lifecycle-safe `correct_trade_signal()` API, including a signal carrying a persisted `BOUGHT`/`SOLD` action or an out-of-list `option_type`, with the correction transaction fully owned by the service. R6.7 adds a validated, decoded `TradeService.list_trade_lifecycle_events()` read API for persisted lifecycle-event evidence - a backend-only service-boundary prerequisite. R7 adds a pure price-return analytics engine and three read-only `TradeService` methods that can compute a lifecycle's win/loss outcome and summarize one trader's performance across every current lifecycle. R8a and R8b (both pushed) built the complete user-facing Trader Performance dashboard: a trader summary table with ranking/comparison and a minimum-sample threshold, a filterable per-trader drill-down, lifecycle/exit-leg evidence, and CSV export - the Product Owner can already view and export trader performance in the running application. R9a added strictly backend, read-only Bulk Channel Import advisory contracts (channel listing/summary, external-id availability, duplicate prediction) with no UI. R9b added the backend authoritative write path - one atomic operation that resolves or creates a channel, ingests a previewed batch, rebuilds only the newly affected lifecycles, and records the operation - also with no UI. Pasting a Discord batch directly through the running application remains not yet possible: that is Recovery Milestone R9c's own explicit, not-yet-started scope. Once R9c ships, a newly imported batch's trades will also need to be reflected in the existing Trader Performance dashboard (R8) on refresh, per R9c's own scope.

Next Action:
This documentation update (this file and the new docs/HANDOFFS/R9b_atomic_bulk_channel_import_handoff.txt) requires Product Owner / Technical Architect (ChatGPT) review before it is staged, committed, or pushed. Once approved, commit and push this documentation update (a separate commit from the already-pushed R9b implementation commit `e9b49df9c00d6d7e9b5d088711932a0d3e22bf6b`), and verify local `main` and `origin/main` remain identical (0 ahead / 0 behind) afterward. The next candidate module is Recovery Milestone R9c (Bulk Channel Import UI); it has no reviewed design yet and is not approved for implementation - it must not begin before this documentation update is synchronized to `origin/main`, and will begin in a new ChatGPT session with its own separate scoping/design planning pass. Do not start R9c automatically.
